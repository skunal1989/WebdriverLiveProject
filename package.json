{
  "name": "webdriver-live-project",
  "type": "module",
  "devDependencies": {
    "@wdio/allure-reporter": "^8.39.0",
    "@wdio/local-runner": "^8.39.0",
    "@wdio/mocha-framework": "^8.39.0",
    "@wdio/spec-reporter": "^8.39.0"
  },
  "scripts": {
    "test": "rimraf allure-report && rimraf allure-result && wdio run ./wdio.conf.js",
    "report": "allure generate --clean ./allure-results &&  allure open"
  },
  "dependencies": {
    "allure-commandline": "^2.29.0"
  }
}
